// import java.util.Stack;

// public class Test {
//     public boolean isSameAfterReversals(int num) {
//         String original = Integer.toString(num);
//         StringBuffer mid = new StringBuffer(original).reverse();
//         int i = 0;
//         while (i < mid.length() && mid.charAt(i) == '0')
//             i++;

//         String last = mid.replace(0, i, "").reverse().toString();
//         return (last.equals(original));

//     }

// }
